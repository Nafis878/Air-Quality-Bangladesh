# Python
__pycache__/
*.py[cod]
.pytest_cache/

# Raw data — not redistributed; place locally per config.yaml
*.xlsx

# Generated artifacts — reproducible via `python -m src.build --config config.yaml`
data/interim/
data/processed/

# Generated model results (regenerate via `python -m src.modeling.run`)
results/
gamma_results.zip

# Provenance copies of the invalid checkpoints — not redistributed (see memory note)
extracted_pth/

# Keep the human-readable report and its figures (data/reports/ is tracked)
